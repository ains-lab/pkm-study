interface:
  display_name: "LLM Wiki"
  short_description: "Build and maintain the project knowledge wiki"
  default_prompt: "Use $llm-wiki to ingest a source, update canonical pages, and run the wiki health checks."
